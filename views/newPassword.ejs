<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>New Password</title>
    <link rel="stylesheet" href="/css/form.css" />
    <link rel="stylesheet" href="/css/index.css" />
    <link rel="stylesheet" href="/css/navbar.css" />
    <script
      src="https://kit.fontawesome.com/e0013384a5.js"
      crossorigin="anonymous"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Noto+Sans:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap"
      rel="stylesheet" />
  </head>
  <body>
    <%- include('partials/navbar.ejs', {user: user}) %>

    <!-- login page code -->
    <div class="login-outer">
      <div class="login-container">
        <div class="login-header">
          <h2><%= heading %></h2>
        </div>
        <form class="login-credential forgot">
          <div>
            <i class="fa-solid fa-lock"></i>
            <input
              id="new-password"
              type="password"
              placeholder="Password"
              autocomplete="off"
              required />
          </div>
          <div>
            <i class="fa-solid fa-lock"></i>
            <input
              id="new-confirmPassword"
              type="password"
              placeholder="Confirm Password"
              autocomplete="off"
              required />
          </div>

          <div class="constraint">
            <ul class="constraint">
              <li>Min. 8 Character long.</li>
              <li>Should have atleast one number.</li>
              <li>Should have atleast lowercase alphabet.</li>
              <li>Should have atleast uppercase alphabet.</li>
            </ul>

            <button class="form-btn" type="button" id="new-btn">update</button>
          </div>

          <p class="info" id="client-info"></p>
          <p class="error" id="client-error"></p>
        </form>
        <hr />
        <div class="wrapper">
          <p>
            New to ShopKart? <a href="/auth/register" class="link">Sign-up</a>
          </p>
        </div>
      </div>
    </div>

    <script type="text/javascript" src="/js/form.js"></script>
    <script type="text/javascript" src="/js/auth.js"></script>
  </body>
</html>
